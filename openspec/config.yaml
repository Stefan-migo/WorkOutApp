# OpenSpec configuration — WorkOutApp
# Auto-generated by sdd-init. Do not edit manually.

context: >
  WorkOutApp: workout timer + planner with intervals, sequences, and calendar.
  Local-first PWA using IndexedDB. Open Design (nexu-io/open-design) for visual
  prototypes. CC0 free-exercise-db for initial exercises, later custom GIFs/images.
  Next.js 14+ App Router + TypeScript + Tailwind CSS v4.

strict_tdd: true

testing:
  runner: vitest
  layers:
    unit: true
    integration: false
    e2e: false
  coverage: false
  tools:
    linter: null
    type_checker: null
    formatter: null

phases:
  proposal: { required: true }
  spec: { required: true }
  design: { required: true }
  tasks: { required: true }
  apply: { required: true }
  verify: { required: true }
  archive: { required: true }

persistence:
  mode: hybrid
  store: both
